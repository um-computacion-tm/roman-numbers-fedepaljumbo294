import unittest

def decimal_to_roman(decimal):
    resultado = ''
    if decimal >= 100:
        centenas -= decimal // 100 # 100 -> centena
        decimal -= centenas * 100 # decimal = nose
        if centenas <= 3:
            resultado = 'C' * centenas
            decimal >= 10:
            decenas = decimal // 10
            if decenas <= 3:
                resultado = 'X' * decenas
            elif decenas <= 4:
                resultado ='XL'
            elif decenas <= 8:
                resultado = 'L'
            elif decenas <= 9:
                resultado = 'XC'
                decimal = decimal = 10
                decimal <= 3:
                resultado 
